'use strict'
/**
 * Protected Route
 * With Authentication
* @author Fajrul
 */
// const userHandler = require('./userHandler')
// const userMiddleware = require('./userMiddleware')
// const {
//   isEntityFalse, clientError400
// } = require('../../core')
module.exports = (app) => {
  // app.post('/generating/hash', [userMiddleware.hashingCheck, isEntityFalse],
  //   userHandler.generateHash, clientError400)
}
