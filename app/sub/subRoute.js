'use strict'

/**
 * Public Route
 * No Authentication
 * @author Fajrul
 */
// const { subController } = require('./index')
module.exports = (app) => {
  /**
   * Sub
   */
  // app.get('/sub', subController.getSub)
  // app.put('/sub', subController.update)
}
